如何使用：
把要解密的epub放到和Main.py同目录下，然后运行Main.py。

将epub内名称被混淆成.*?之类的文件及其索引路径恢复成普通名称，使得阅读软件可正常读取。
该类混淆的例子有:比如基于多看标准制作的加密epub，使得只能在在多看上看而其它软件会打不开或提示drm保护。

须知：
由于windows系统有不允许使用*?:/等符号作为文件名的限制，
那么内部的乱码文件在解包后就立刻会被系统自动丢弃导致无法正常编辑，
因此该脚本无法在win系统上正常生效，需在安卓等允许此类文件名的平台使用该脚本。
